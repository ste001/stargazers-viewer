# Stargazers Viewer

## Configuration

- Create a .env file
- Copy the contents of the .env.example file inside the .env file
- Add the provided environment variables

## Run / Test

Run the app

`yarn start`

Run the tests

`yarn test`